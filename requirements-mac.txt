# Requirements for Mac (Apple Silicon) with MLX
# Optimized for M1/M2/M3/M4 Macs with 24GB+ unified memory
# Uses 4-bit quantized models for efficient inference

# MLX framework for Apple Silicon
mlx>=0.20.0
mlx-vlm>=0.1.0  # Vision-language models for MLX

# Core dependencies (same as main)
torch>=2.0.0  # Will use MPS backend on Mac
torchvision
Pillow>=10.0.0
numpy>=1.24.0
tqdm
pyyaml

# Video processing
opencv-python>=4.8.0

# Audio processing
openai-whisper>=20231117

# OCR (CPU-only for Mac)
paddlepaddle  # CPU version
paddleocr>=2.7.0

# Text processing
sentence-transformers>=2.2.0

# Optional: for Gradio web interface
gradio>=4.0.0

# Note: On Mac, run these commands after installing:
# 1. pip install -r requirements-mac.txt
# 2. The first run will download the MLX model (~4GB for 4-bit Qwen)
